# TrendGhost needs no secrets: everything runs on-device and nothing is uploaded.
# This file exists so that stays true — if a variable ever appears, it gets documented here.

# Optional: override where the MediaPipe pose model files are served from.
# Default is /models (populated by `npm run fetch-models`).
# VITE_MODEL_BASE_URL=/models
