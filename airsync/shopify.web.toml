# Tells `shopify app dev` how to run this app (the CLI's per-process config).
name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npm exec prisma generate"
dev = "npm exec prisma migrate deploy && npm exec remix vite:dev"
