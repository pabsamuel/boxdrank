// Constants shared between server code and the browser UI.
// (Must NOT import anything server-only — this file ships to the client.)

// Free plan: how many new orders can sync per calendar month.
export const FREE_PLAN_ORDER_LIMIT = 50;
