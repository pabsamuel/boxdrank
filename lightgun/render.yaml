services:
  - type: web
    name: lightgun
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /healthz
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
